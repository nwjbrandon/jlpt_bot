services:
  jp-telegram-bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: jp-telegram-bot:latest
    container_name: jp-telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data:ro
