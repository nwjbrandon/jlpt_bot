services:
  jp-line-bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: jp-line-bot:latest
    container_name: jp-line-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data:ro
